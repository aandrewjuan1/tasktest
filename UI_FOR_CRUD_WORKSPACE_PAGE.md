# UI for CRUD Workspace Page

## List View
-Two ways to create or show the create form:
    -Add button, upon clicking shows a modal
    -CTA to add is inside the card container, so it's like a create a card that is empty and only has "+" icon inside it
-
